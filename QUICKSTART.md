# 🚀 Быстрый старт - Design Emotion

## ⚡ Установка и запуск за 5 минут

### 1️⃣ Установка зависимостей

```bash
cd C:\dev\de-web
npm install
```

### 2️⃣ Настройка базы данных

```bash
# Генерация Prisma Client
npm run db:generate
```

### 3️⃣ Запуск приложений

**Запустить все (web + admin):**
```bash
npm run dev
```

**Или по отдельности:**

```bash
# Только публичный сайт (порт 3000)
npm run dev:web

# Только админка (порт 3001)
npm run dev:admin
```

### 4️⃣ Открыть в браузере

- 🌐 **Публичный сайт:** http://localhost:3000
- 🔧 **Админка:** http://localhost:3001/dream

---

## 📂 Структура проекта

```
C:\dev\de-web\
├── apps\
│   ├── web\                    # Публичный сайт (порт 3000)
│   │   └── src\app\
│   │       └── page.tsx        # Главная страница
│   │
│   └── admin\                  # Админка (порт 3001)
│       └── src\app\dream\
│           ├── page.tsx        # Dashboard
│           ├── users\          # Управление пользователями
│           ├── emotions\       # 108 эмоций
│           ├── course\         # Дневник эмоций
│           └── analytics\      # Аналитика
│
└── packages\
    └── database\               # Shared Prisma schema
        ├── schema.prisma       # Схема БД
        └── index.ts            # Prisma Client
```

---

## 🎯 Основные команды

### Development
```bash
npm run dev              # Запустить все
npm run dev:web          # Только web
npm run dev:admin        # Только admin
```

### Database
```bash
npm run db:generate      # Генерация Prisma Client
npm run db:push          # Push схемы в БД
npm run db:studio        # Открыть Prisma Studio (GUI для БД)
```

### Build
```bash
npm run build            # Собрать все
npm run build:web        # Только web
npm run build:admin      # Только admin
```

---

## 🔧 Если что-то не работает

### ❌ Ошибка: Cannot find module '@designemotion/database'

**Решение:**
```bash
npm run db:generate
```

### ❌ Порт 3000 или 3001 занят

**Windows:**
```bash
netstat -ano | findstr :3000
taskkill /PID <PID> /F
```

### ❌ Cannot connect to database

**Проверка подключения:**
```bash
ping 185.185.142.167
```

---

## 📊 Доступные страницы админки

| URL | Описание |
|-----|----------|
| `/dream` | Dashboard с общей статистикой |
| `/dream/users` | Список пользователей бота |
| `/dream/emotions` | Управление 108 эмоциями |
| `/dream/course` | Курс "Дневник эмоций" |
| `/dream/analytics` | Аналитика и графики |

---

**Готово! Теперь ты можешь работать с проектом.** 🎉
