# 🧩 Landing Blocks System - Design Emotions

**Дата создания:** 12 ноября 2025  
**Назначение:** Универсальные переиспользуемые блоки для лендингов

---

## 📦 Блоки (8 компонентов)

### 1. **HeroBlock** - Главный экран

**Назначение:** Первый экран с headline, результатами, ценой и CTA

**Props:**
```typescript
{
  targeting: string;         // "Для владельцев салонов • Москва"
  headline: string;          // Главный заголовок
  subheadline: string;       // Подзаголовок
  results: string[];         // Список результатов (✓)
  price: string;             // "10,000 ₽"
  priceNote?: string;        // "90 минут онлайн"
  urgency?: string;          // "3 места на эту неделю"
  ctaText?: string;          // Текст кнопки
  source: string;            // Для аналитики
  stats?: Array<{            // Статистика (опционально)
    label: string;
    value: string;
  }>;
  background?: 'graphite' | 'ivory' | 'gradient';
}
```

**Пример:**
```tsx
<HeroBlock
  targeting="Для владельцев салонов • Москва и СПб"
  headline="Ваш «мастер-звезда» держит вас в заложниках?"
  subheadline="90-минутная сессия 'Дизайн Эмоций'"
  results={[
    '✓ Ясность: поймете корень проблемы',
    '✓ Облегчение: -60% напряжения',
    '✓ План: 2-3 конкретных шага'
  ]}
  price="10,000 ₽"
  urgency="3 места на эту неделю"
  source="beauty-leaders"
  background="gradient"
/>
```

---

### 2. **PainBlock** - Боли и проблемы

**Назначение:** Секция с болевыми точками аудитории

**Props:**
```typescript
{
  title?: string;            // "Узнаете себя?"
  subtitle?: string;         // Описание секции
  pains: Array<{
    title: string;
    description: string;
  }>;
  ctaText?: string;          // "Узнали хотя бы одну?"
  ctaButtonText?: string;    // "Хочу разобраться"
  source: string;            // Для аналитики
  background?: 'ivory' | 'white' | 'graphite';
}
```

**Пример:**
```tsx
<PainBlock
  title="Узнаете себя?"
  pains={[
    {
      title: '"Мастер-звезда" держит в заложниках',
      description: 'Он приносит 40% выручки...'
    },
    // ...
  ]}
  source="beauty-leaders"
  background="ivory"
/>
```

---

### 3. **TestimonialsBlock** - Отзывы клиентов

**Назначение:** Социальное доказательство

**Props:**
```typescript
{
  title?: string;            // "Что говорят клиенты"
  testimonials: Array<{
    text: string;
    author: string;
    role: string;
    avatar?: string;         // URL (опционально)
  }>;
  background?: 'white' | 'ivory' | 'graphite';
  columns?: 1 | 2 | 3;       // Количество колонок
}
```

**Пример:**
```tsx
<TestimonialsBlock
  testimonials={[
    {
      text: 'После одной сессии я поняла...',
      author: 'Анна',
      role: 'Владелица салона в Москве'
    }
  ]}
  columns={2}
  background="white"
/>
```

---

### 4. **GuaranteeBlock** - Гарантия

**Назначение:** Снятие страхов через гарантию возврата

**Props:**
```typescript
{
  title?: string;            // "Гарантия результата"
  description: string;       // Текст гарантии
  highlight?: string;        // Выделенный текст
  background?: 'ivory' | 'white' | 'graphite';
  icon?: React.ReactNode;    // Кастомная иконка
}
```

**Пример:**
```tsx
<GuaranteeBlock
  description="Если после 30 минут вы поймете, что метод не для вас — я верну 100% оплаты."
  highlight="Без вопросов и объяснений."
  background="ivory"
/>
```

---

### 5. **FAQBlock** - Частые вопросы

**Назначение:** Закрытие возражений

**Props:**
```typescript
{
  title?: string;            // "Частые вопросы"
  items: Array<{
    question: string;
    answer: string;
  }>;
  background?: 'white' | 'ivory' | 'graphite';
}
```

**Пример:**
```tsx
<FAQBlock
  items={[
    {
      question: 'Чем отличается от психолога?',
      answer: 'Психолог исследует прошлое...'
    }
  ]}
  background="white"
/>
```

---

### 6. **TransformationBlock** - Эффект домино

**Назначение:** Показать побочные эффекты трансформации

**Props:**
```typescript
{
  title?: string;            // "Эффект домино"
  subtitle?: string;         // Описание
  transformations: Array<{
    before: string;          // "В работе:"
    after: string;           // "В жизни:"
  }>;
  finalMessage: string;      // Итоговое сообщение
  background?: 'ivory' | 'white' | 'graphite';
}
```

**Пример:**
```tsx
<TransformationBlock
  transformations={[
    {
      before: 'Умение отделить свою ценность от выручки',
      after: 'Перестанете зависеть от чужого одобрения'
    }
  ]}
  finalMessage="Вы приходите стать эффективным лидером. Вы уходите свободным человеком."
  background="ivory"
/>
```

---

### 7. **PhilosophyBlock** - Философия метода

**Назначение:** Объяснить подход и отличия

**Props:**
```typescript
{
  title: string;             // Заголовок (поддерживает \n)
  paragraphs: string[];      // Абзацы текста
  highlight?: {              // Выделенный блок
    title: string;
    text: string;
  };
  background?: 'graphite' | 'ivory' | 'white';
}
```

**Пример:**
```tsx
<PhilosophyBlock
  title="От управления персоналом\nк архитектуре состояний"
  paragraphs={[
    'Большинство руководителей пытаются...',
    'Метод "Дизайн Эмоций" — это...'
  ]}
  highlight={{
    title: 'Ключевой принцип:',
    text: 'Ваша команда не читает приказы. Она считывает состояние.'
  }}
  background="graphite"
/>
```

---

### 8. **CTABlock** - Финальный призыв

**Назначение:** Последний шанс конвертировать

**Props:**
```typescript
{
  title: string;             // "Готовы к трансформации?"
  subtitle: string;          // Описание оффера
  price: string;             // "10,000 ₽"
  priceNote?: string;        // "90 минут онлайн"
  urgency?: string;          // "3 места на эту неделю"
  ctaText?: string;          // Текст кнопки
  source: string;            // Для аналитики
  background?: 'graphite' | 'bronze' | 'ivory';
}
```

**Пример:**
```tsx
<CTABlock
  title="Готовы к трансформации?"
  subtitle="Первый шаг — 90-минутная сессия"
  price="10,000 ₽"
  priceNote="90 минут онлайн • С гарантией"
  urgency="3 места на эту неделю"
  source="beauty-leaders"
  background="graphite"
/>
```

---

## 🎨 Цветовая палитра

```typescript
// Основные
graphite: '#2B2B2B'      // Темно-серый (основной)
ivory: '#FAF8F3'         // Слоновая кость (светлый фон)
bronze: '#CD7F32'        // Бронзовый (акцент)
white: '#FFFFFF'         // Белый

// Использование в блоках
background?: 'graphite' | 'ivory' | 'white' | 'bronze' | 'gradient'
```

---

## 📐 Типичная структура лендинга

```tsx
import {
  HeroBlock,
  PainBlock,
  PhilosophyBlock,
  TestimonialsBlock,
  GuaranteeBlock,
  TransformationBlock,
  FAQBlock,
  CTABlock,
} from '@/components/landing-blocks';

export default function LandingPage() {
  return (
    <>
      <Header />
      <main className="pt-20">
        
        {/* 1. Hero - первое впечатление */}
        <HeroBlock {...heroProps} />
        
        {/* 2. Боли - резонанс с аудиторией */}
        <PainBlock {...painProps} />
        
        {/* 3. Философия - объяснить подход */}
        <PhilosophyBlock {...philosophyProps} />
        
        {/* 4. Отзывы - социальное доказательство */}
        <TestimonialsBlock {...testimonialsProps} />
        
        {/* 5. Гарантия - снять страхи */}
        <GuaranteeBlock {...guaranteeProps} />
        
        {/* 6. Трансформация - побочные эффекты */}
        <TransformationBlock {...transformationProps} />
        
        {/* 7. FAQ - закрыть возражения */}
        <FAQBlock {...faqProps} />
        
        {/* 8. CTA - финальный призыв */}
        <CTABlock {...ctaProps} />
        
      </main>
      <Footer />
    </>
  );
}
```

---

## ✅ Преимущества системы

### 1. **DRY (Don't Repeat Yourself)**
- Один компонент → много лендингов
- Исправление бага в одном месте → везде

### 2. **Консистентность**
- Единый дизайн
- Одинаковые отступы, шрифты, цвета
- Predictable UX

### 3. **Скорость разработки**
- Новый лендинг за 30 минут
- Не нужно писать CSS с нуля
- Копипаст данных, не кода

### 4. **Гибкость**
- Props для кастомизации
- background, columns, icons
- Легко добавить новые варианты

### 5. **Аналитика**
- source prop в каждом блоке
- Отслеживание конверсий по секциям
- A/B тесты через props

---

## 🔧 Технические детали

**Стек:**
- React 18 ('use client')
- TypeScript
- TailwindCSS
- Lucide React (иконки)

**Интеграция:**
- LeadFormPopup для всех CTA
- Yandex Metrika goals
- Google Analytics events

**Responsive:**
- Mobile-first
- Breakpoints: md (768px), lg (1024px)
- Grid columns адаптивные

---

## 📝 Примеры использования

### Пример 1: Beauty Leaders (текущий)
```tsx
<HeroBlock {...} />
<PainBlock pains={beautyPains} />
<PhilosophyBlock {...} />
<TestimonialsBlock testimonials={beautyTestimonials} />
<GuaranteeBlock {...} />
<TransformationBlock {...} />
<FAQBlock items={beautyFAQ} />
<CTABlock {...} />
```

### Пример 2: Dizayn Emotsiy (будущий)
```tsx
<HeroBlock 
  targeting="Для предпринимателей • Вся Россия"
  headline="Устали от эмоциональных качелей?"
  // ...
/>
<PainBlock pains={entrepreneurPains} />
// ...
```

### Пример 3: Navigator 2026 (будущий)
```tsx
<HeroBlock 
  targeting="Для тех, кто планирует 2026"
  headline="2025 прошел... так себе?"
  // ...
/>
// ...
```

---

## 🚀 Roadmap

### ✅ Phase 1: Базовые блоки (DONE)
- [x] HeroBlock
- [x] PainBlock
- [x] TestimonialsBlock
- [x] GuaranteeBlock
- [x] FAQBlock
- [x] TransformationBlock
- [x] PhilosophyBlock
- [x] CTABlock

### 🔜 Phase 2: Расширенные блоки
- [ ] PricingBlock (сравнение тарифов)
- [ ] TimelineBlock (процесс работы)
- [ ] StatsBlock (цифры и достижения)
- [ ] VideoBlock (видео-отзывы)
- [ ] ComparisonBlock (До/После)

### 🔜 Phase 3: Интерактивные блоки
- [ ] QuizBlock (встроенный тест)
- [ ] CalculatorBlock (калькулятор ROI)
- [ ] ChatBlock (чат с AI)

---

## 📚 Связанная документация

- `C:\dev\de-docs\business\new site\yandex_direct_strategy.md` - стратегия рекламы
- `C:\dev\de-docs\business\new site\de_content_spec.md` - контент-стратегия
- `C:\dev\de-web\apps\web\src\components\lead-form\LeadFormPopup.tsx` - форма заявок

---

**Статус:** ✅ Production Ready  
**Последнее обновление:** 12 ноября 2025  
**Автор:** Design Emotions Team
